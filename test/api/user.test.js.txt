const bcrypt = require('bcrypt');
const { createUser } = require('../../src/controller/user'); // Supondo que sua função createUser esteja em um arquivo user.js

describe('Testes da função createUser', () => {

    it('Deve lançar um erro se a senha estiver vazia', async () => {
        // Arrange
        const nome = 'UserTest';
        const email = 'teste@gmail.com';
        const senha = '';

        // Act & Assert
        await expect(createUser(nome, email, senha)).rejects.toThrow();
    });

});
